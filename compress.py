import os
#Where am I checking??? Documents/YouTubeDonwloads

#needs to look for target directory before executing script. If found run function else throw error
#Looks for ready_for_ide directory, if not exists create then execute, else just execute
#needs to loop through YTDownload directory and run main function on each mp4
#Main function is to convert the file into a tar.bz2 then place it in another directory